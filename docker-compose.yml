api:
  build: ./
  ports:
    - 9550:5000
  volumes:
    - ./:/go/src/github.com/LordRahl90/addressbookapi

  links:
    - database

  environment:
    - DB_NAME=addressbook

database:
  image: mongo:latest
  command: mongod --smallfiles --quiet --logpath=/dev/null